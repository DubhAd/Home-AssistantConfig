id: 'healthcheck'
initial_state: 'on'
alias: 'Healthcheck'
trigger:
  - platform: webhook
    webhook_id: !secret pi3
action:
  - service: notify.debug_log
    data:
      message: >
        {{ trigger.data }}
