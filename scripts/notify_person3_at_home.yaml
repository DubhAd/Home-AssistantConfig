alias: Notify person3 at home
sequence:
  - condition: state
    entity_id: input_boolean.person3_home
    state: 'on'
  - service: notify.command_line_person3
    data:
      message: "{{ message }}"

